@model System.Collections.Generic.List<Shopping_Cart.ViewModels.BookViewModel>
@{
    ViewBag.Title = "Livros";
}
<h2>Livros</h2>
@if (Model.Count > 0)
{
    @Html.Partial("_List",Model)
}
else
{
    <div class="alert alert-info">
        Não há livros disponíveis.
    </div>
}